-- Users sorted by age ascending
SELECT * FROM users
ORDER BY age;

-- Users sorted by age descending
SELECT * FROM users
ORDER BY age DESC;

-- Products sorted by price descending
SELECT * FROM products
ORDER BY price DESC;

-- Products sorted by stock quantity ascending
SELECT * FROM products
ORDER BY stock_quantity;
